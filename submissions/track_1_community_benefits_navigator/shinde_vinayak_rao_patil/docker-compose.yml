services:
  navigator:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - backend/.env
    volumes:
      - ./backend/data:/app/backend/data
      - chroma_data:/app/chroma_db
    restart: unless-stopped

volumes:
  chroma_data:
